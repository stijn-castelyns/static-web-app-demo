{
    "routes": [
        {
            "route": "/about.html",
            "allowedRoles": [
                "authenticated"
            ]
        },
        {
            "route": "/logout",
            "rewrite": "/.auth/logout"
        }
    ]
}